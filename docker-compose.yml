version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: dotnet-react-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dotnet_react_template
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./src/Backend
      dockerfile: Dockerfile
    container_name: dotnet-react-backend
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      # Secrets Management - Environment Variables (override these in production)
      - DATABASE_URL=Host=postgres;Database=dotnet_react_template;Username=postgres;Password=postgres
      - JWT_SECRET_KEY=YourSuperSecretKeyThatIsAtLeast32CharactersLong!ChangeInProduction
      - JWT_ISSUER=DotnetReactTemplate
      - JWT_AUDIENCE=DotnetReactTemplateUsers
      - JWT_ACCESS_TOKEN_EXPIRATION_MINUTES=15
      - JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7
      # Infisical (uncomment to use)
      # - USE_INFISICAL=true
      # - INFISICAL_TOKEN=${INFISICAL_TOKEN}
      # - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      # - INFISICAL_ENVIRONMENT=dev
    volumes:
      - ./src/Backend:/app
      - /app/bin
      - /app/obj
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    build:
      context: ./src/Frontend
      dockerfile: Dockerfile
    container_name: dotnet-react-frontend
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://localhost:8080
    volumes:
      - ./src/Frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
